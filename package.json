{
  "name": "courses-test-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir dist --public-url ./ && cp -r src/static dist/static",
    "clean": "rm -rf dist .parcel-cache"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.3",
    "parcel": "^2.10.3"
  }
}